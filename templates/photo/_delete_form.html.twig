<form method="post" action="{{ path('app_photo_delete', {'id': image.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette photo?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
